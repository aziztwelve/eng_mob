# API Configuration — physical device via adb reverse
# Android emulator → host machine
# EXPO_PUBLIC_API_URL=http://localhost:8081/api/v1

# Physical device via adb reverse tcp:8080 tcp:8080
EXPO_PUBLIC_API_URL=https://api.lingoiq.online/api/v1
